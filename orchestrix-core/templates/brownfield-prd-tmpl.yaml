template:
  id: brownfield-prd-template-v2
  name: Brownfield Enhancement PRD
  version: 2.0
  output:
    format: markdown
    filename: docs/prd.md
    title: "{{project_name}} Brownfield Enhancement PRD"

workflow:
  mode: draft-first
  elicitation: advanced-elicitation

sections:
  - id: intro-analysis
    title: Intro Project Analysis and Context
    instruction: |
      IMPORTANT - SCOPE AND PREREQUISITES:

      This PRD is for SIGNIFICANT enhancements to existing projects that require comprehensive planning and multiple stories. Before proceeding:

      1. **Assess Enhancement Complexity**: If this is a simple feature addition or bug fix that could be completed in 1-2 focused development sessions, STOP and recommend: "For simpler changes, consider using the brownfield-create-epic or brownfield-create-story task with the Product Owner instead. This full PRD process is designed for substantial enhancements that require architectural planning and multiple coordinated stories."

      2. **REQUIRED PREREQUISITE - Existing System Analysis**:
         - **Multi-Repo Detection**: Check if core-config.yaml contains `multi_repo:` key with `mode: multi-repo`
           * If YES: Multi-repository mode → **MUST have**: docs/existing-system-integration.md (created via @architect *aggregate-system-analysis)
           * If NO: Single-repository mode → **MUST have**: docs/existing-system-analysis.md (created via @architect *document-project)
         - **Why required**: This PRD defines ENHANCEMENTS to the existing system. You need to understand the current state first.
         - **If missing**:
           * Multi-repo: HALT and instruct: "Please run '@architect *aggregate-system-analysis' first to create docs/existing-system-integration.md. This aggregates analysis from all implementation repositories."
           * Single-repo: HALT and instruct: "Please run '@architect *document-project' first to create docs/existing-system-analysis.md. This provides the necessary context about the existing system before defining enhancements."

      3. **Project Context Validation**:
         - **Auto-detect mode**: Check core-config.yaml for `project.mode` and `multi_repo` configuration
         - **Load and review**:
           * Multi-repo mode: docs/existing-system-integration.md (aggregated analysis)
           * Single-repo mode: docs/existing-system-analysis.md (repository analysis)
         - Verify understanding of current tech stack, architecture, and constraints
         - If IDE access available, validate analysis against actual codebase

      4. **Deep Assessment Requirement**: You MUST thoroughly understand the existing project (from analysis document) before making ANY enhancement suggestions. Every recommendation must consider existing constraints and patterns.

      Gather comprehensive information about the existing project. This section must be completed before proceeding with requirements.

      CRITICAL: Throughout this process, explicitly reference findings from the analysis document. For every enhancement suggestion, explain: "Based on the existing system analysis showing [constraint/pattern], I recommend [enhancement] because [rationale]. Does this align with your goals?"

      Do not proceed with any recommendations until the user has validated your understanding of the existing system.
    sections:
      - id: existing-project-overview
        title: Existing Project Overview
        instruction: Check if document-project analysis was already performed. If yes, reference that output instead of re-analyzing.
        sections:
          - id: analysis-source
            title: Analysis Source
            instruction: |
              Reference the existing system analysis document (auto-detected):
              - Primary source:
                * Multi-repo mode: docs/existing-system-integration.md (aggregated from all repos)
                * Single-repo mode: docs/existing-system-analysis.md (current repository)
              - Supplementary: IDE-based validation (if available)
              - Additional context: User-provided information

              Auto-detect by checking core-config.yaml for `project.mode` value.
          - id: current-state
            title: Current Project State
            instruction: |
              Extract summary from analysis document (auto-detected):
              - **Multi-repo mode**: Extract from docs/existing-system-integration.md
                * Reference "Repository Topology" section
                * Note "Cross-Repository API Contracts"
                * Highlight "Technical Debt (System-Level)" section
              - **Single-repo mode**: Extract from docs/existing-system-analysis.md
                * Reference "High Level Architecture" and "Technical Summary" sections
                * Note key technologies from "Tech Stack" section
                * Highlight constraints from "Technical Debt and Known Issues" section
              - Brief description of what the project currently does and its primary purpose
      - id: documentation-analysis
        title: Available Documentation Analysis
        instruction: |
          Review analysis document (REQUIRED prerequisite, auto-detected):
          - **Multi-repo mode**: docs/existing-system-integration.md
            * Note: "Using aggregated system integration analysis"
            * Reference: Repository Topology, API Contracts, Integration Patterns
          - **Single-repo mode**: docs/existing-system-analysis.md
            * Note: "Using existing system analysis"
            * Reference: Tech Stack, Source Tree, Coding Standards, Technical Debt

          Additionally check for other existing documentation in the project:
        sections:
          - id: available-docs
            title: Available Documentation
            type: checklist
            items:
              - Tech Stack Documentation [[LLM: Check ✓ from existing-system-analysis.md]]
              - Source Tree/Architecture [[LLM: Check ✓ from existing-system-analysis.md]]
              - Coding Standards [[LLM: Check ✓ from existing-system-analysis.md]]
              - API Documentation [[LLM: Check ✓ if present in existing-system-analysis.md]]
              - External API Documentation [[LLM: Check ✓ if present in existing-system-analysis.md]]
              - UX/UI Guidelines [[LLM: May not be in existing-system-analysis.md]]
              - Technical Debt Documentation [[LLM: Check ✓ from existing-system-analysis.md]]
              - "Other: {{other_docs}}"
            instruction: |
              Using existing system analysis from docs/existing-system-analysis.md as primary source.
              Mark items as available (✓) if covered in existing-system-analysis.md.
      - id: enhancement-scope
        title: Enhancement Scope Definition
        instruction: Work with user to clearly define what type of enhancement this is. This is critical for scoping and approach.
        sections:
          - id: enhancement-type
            title: Enhancement Type
            type: checklist
            instruction: Determine with user which applies
            items:
              - New Feature Addition
              - Major Feature Modification
              - Integration with New Systems
              - Performance/Scalability Improvements
              - UI/UX Overhaul
              - Technology Stack Upgrade
              - Bug Fix and Stability Improvements
              - "Other: {{other_type}}"
          - id: enhancement-description
            title: Enhancement Description
            instruction: 2-3 sentences describing what the user wants to add or change
          - id: impact-assessment
            title: Impact Assessment
            type: checklist
            instruction: Assess the scope of impact on existing codebase
            items:
              - Minimal Impact (isolated additions)
              - Moderate Impact (some existing code changes)
              - Significant Impact (substantial existing code changes)
              - Major Impact (architectural changes required)
      - id: goals-context
        title: Goals and Background Context
        sections:
          - id: goals
            title: Goals
            type: bullet-list
            instruction: Bullet list of 1-line desired outcomes this enhancement will deliver if successful
          - id: background
            title: Background Context
            type: paragraphs
            instruction: 1-2 short paragraphs explaining why this enhancement is needed, what problem it solves, and how it fits with the existing project
      - id: changelog
        title: Change Log
        type: table
        columns: [Change, Date, Version, Description, Author]

  - id: requirements
    title: Requirements
    instruction: |
      Draft functional and non-functional requirements based on your validated understanding of the existing project. Before presenting requirements, confirm: "These requirements are based on my understanding of your existing system. Please review carefully and confirm they align with your project's reality."
    elicit: true
    sections:
      - id: functional
        title: Functional
        type: numbered-list
        prefix: FR
        instruction: Each Requirement will be a bullet markdown with identifier starting with FR
        examples:
          - "FR1: The existing Todo List will integrate with the new AI duplicate detection service without breaking current functionality."
      - id: non-functional
        title: Non Functional
        type: numbered-list
        prefix: NFR
        instruction: Each Requirement will be a bullet markdown with identifier starting with NFR. Include constraints from existing system
        examples:
          - "NFR1: Enhancement must maintain existing performance characteristics and not exceed current memory usage by more than 20%."
      - id: compatibility
        title: Compatibility Requirements
        instruction: Critical for brownfield - what must remain compatible
        type: numbered-list
        prefix: CR
        template: "{{requirement}}: {{description}}"
        items:
          - id: cr1
            template: "CR1: {{existing_api_compatibility}}"
          - id: cr2
            template: "CR2: {{database_schema_compatibility}}"
          - id: cr3
            template: "CR3: {{ui_ux_consistency}}"
          - id: cr4
            template: "CR4: {{integration_compatibility}}"

  - id: ui-enhancement-goals
    title: User Interface Enhancement Goals
    condition: Enhancement includes UI changes
    instruction: For UI changes, capture how they will integrate with existing UI patterns and design systems
    sections:
      - id: existing-ui-integration
        title: Integration with Existing UI
        instruction: Describe how new UI elements will fit with existing design patterns, style guides, and component libraries
      - id: modified-screens
        title: Modified/New Screens and Views
        instruction: List only the screens/views that will be modified or added
      - id: ui-consistency
        title: UI Consistency Requirements
        instruction: Specific requirements for maintaining visual and interaction consistency with existing application

  - id: technical-constraints
    title: Technical Constraints and Integration Requirements
    instruction: This section replaces separate architecture documentation. Gather detailed technical constraints from existing project analysis.
    sections:
      - id: existing-tech-stack
        title: Existing Technology Stack
        instruction: |
          If document-project output available:
          - Extract from "Actual Tech Stack" table in High Level Architecture section
          - Include version numbers and any noted constraints
          
          Otherwise, document the current technology stack:
        template: |
          **Languages**: {{languages}}
          **Frameworks**: {{frameworks}}
          **Database**: {{database}}
          **Infrastructure**: {{infrastructure}}
          **External Dependencies**: {{external_dependencies}}
      - id: integration-approach
        title: Integration Approach
        instruction: Define how the enhancement will integrate with existing architecture
        template: |
          **Database Integration Strategy**: {{database_integration}}
          **API Integration Strategy**: {{api_integration}}
          **Frontend Integration Strategy**: {{frontend_integration}}
          **Testing Integration Strategy**: {{testing_integration}}
      - id: code-organization
        title: Code Organization and Standards
        instruction: Based on existing project analysis, define how new code will fit existing patterns
        template: |
          **File Structure Approach**: {{file_structure}}
          **Naming Conventions**: {{naming_conventions}}
          **Coding Standards**: {{coding_standards}}
          **Documentation Standards**: {{documentation_standards}}
      - id: deployment-operations
        title: Deployment and Operations
        instruction: How the enhancement fits existing deployment pipeline
        template: |
          **Build Process Integration**: {{build_integration}}
          **Deployment Strategy**: {{deployment_strategy}}
          **Monitoring and Logging**: {{monitoring_logging}}
          **Configuration Management**: {{config_management}}
      - id: risk-assessment
        title: Risk Assessment and Mitigation
        instruction: |
          If document-project output available:
          - Reference "Technical Debt and Known Issues" section
          - Include "Workarounds and Gotchas" that might impact enhancement
          - Note any identified constraints from "Critical Technical Debt"
          
          Build risk assessment incorporating existing known issues:
        template: |
          **Technical Risks**: {{technical_risks}}
          **Integration Risks**: {{integration_risks}}
          **Deployment Risks**: {{deployment_risks}}
          **Mitigation Strategies**: {{mitigation_strategies}}

  - id: epic-structure
    title: Epic and Story Structure
    instruction: |
      For brownfield projects, favor a single comprehensive epic unless the user is clearly requesting multiple unrelated enhancements. Before presenting the epic structure, confirm: "Based on my analysis of your existing project, I believe this enhancement should be structured as [single epic/multiple epics] because [rationale based on actual project analysis]. Does this align with your understanding of the work required?"
    elicit: true
    sections:
      - id: epic-approach
        title: Epic Approach
        instruction: Explain the rationale for epic structure - typically single epic for brownfield unless multiple unrelated features
        template: "**Epic Structure Decision**: {{epic_decision}} with rationale"

  - id: epics
    title: Epics
    instruction: |
      ⚠️ CRITICAL SECTION - Complete requirement specifications for all Epics and Stories.

      ## Purpose

      This section contains COMPLETE requirement details that flow directly to Dev agents.
      PM MUST define all business rules, validation, error handling here - NOT delegated to SM.
      SM agents will render this content into Story documents WITHOUT additional analysis.

      ## Story Sequencing Requirements (Brownfield-Specific)

      - Stories must ensure existing functionality remains intact
      - Each story should include verification that existing features still work
      - Stories should be sequenced to minimize risk to existing system
      - Include rollback considerations for each story
      - Focus on incremental integration rather than big-bang changes
      - Size stories for AI agent execution in existing codebase context
      - No story depends on work from a later story or epic
      - MANDATORY: Present the complete story sequence and ask: "Does this order make sense given your project's architecture?"

      During PO *shard, YAML blocks will be extracted to docs/prd/epic-{n}-{title}.yaml for SM agents.

      ## Epic Document Format

      For EACH epic, create a YAML block using this EXACT structure:

      ```
      ## Epic {{N}}: {{Epic Title}}

      **Epic Summary:** {{2-3 sentence summary}}

      **Target Repositories:** {{backend, frontend, ios, android, mobile - as applicable}}

      **Integration Notes:** {{Brief note on how this integrates with existing system}}
      ```

      Followed by a YAML code fence with the complete Epic definition.

      ## Enhanced Acceptance Criteria Structure

      CRITICAL: Each AC is a COMPLETE requirement unit. Dev agents will implement directly
      from this specification without needing to consult PRD or architecture documents.

      ```yaml
      epic_id: {{N}}
      title: "{{Epic Title}}"
      description: |
        {{Epic goal - 2-3 sentences describing objective and integration requirements}}

      # ============================================================
      # REUSE ANALYSIS - Existing code assessment for brownfield
      # Brownfield projects SHOULD always populate this section
      # Records existing implementations to reuse/extend/resolve
      # ============================================================
      reuse_analysis:
        directly_reusable:
          # Category A: Existing code to reference, NOT reimplement
          - component: "{{component_name}}"
            location: "{{file_path}}"
            capability: "{{what_it_does}}"
            usage: "{{how_this_epic_uses_it}}"

        requires_extension:
          # Category B: Existing code to extend
          - component: "{{component_name}}"
            location: "{{file_path}}"
            current_capability: "{{what_it_does_now}}"
            extension_needed: "{{what_to_add}}"
            affected_stories: ["{{story_id}}"]

        conflicts:
          # Category C: Conflicts requiring resolution
          - component: "{{component_name}}"
            location: "{{file_path}}"
            conflict: "{{conflict_description}}"
            resolution: "{{chosen_resolution}}"
            affected_stories: ["{{story_id}}"]

        new_implementations:
          # Category D: New code to create
          - feature: "{{feature_name}}"
            suggested_location: "{{file_path}}"
            pattern_reference: "{{existing_file_to_follow}}"
            affected_stories: ["{{story_id}}"]

      stories:
        - id: "{{N}}.1"
          title: "{{Story title}}"
          repository_type: backend | frontend | ios | android | monolith
          estimated_complexity: low | medium | high
          priority: P0 | P1 | P2

          # ============================================================
          # ENHANCED ACCEPTANCE CRITERIA - Complete Requirement Specs
          # ============================================================
          acceptance_criteria:
            - id: AC1
              title: "{{Concise AC title}}"

              # REQUIRED: Scenario in GIVEN/WHEN/THEN format
              scenario:
                given: "{{Precondition state}}"
                when: "{{User action or system trigger}}"
                then:
                  - "{{Expected outcome 1}}"
                  - "{{Expected outcome 2}}"

              # REQUIRED: Business rules governing this AC
              # Minimum 1 rule per AC. Use BR-{ac_num}.{seq} format.
              business_rules:
                - id: "BR-1.1"
                  rule: "{{Business rule description}}"

              # CONDITIONAL: Required when AC involves form input or API request body
              data_validation:
                - field: "{{field_name}}"
                  type: "string | number | boolean | email | date | array | object"
                  required: true | false
                  rules: "{{Validation rules}}"
                  error_message: "{{User-facing error message}}"

              # REQUIRED: Error scenarios and handling
              # Minimum 1 error scenario per AC
              error_handling:
                - scenario: "{{Error condition description}}"
                  code: "{{HTTP status or error code}}"
                  message: "{{User-facing error message}}"
                  action: "{{System behavior on this error}}"

              # OPTIONAL: UI interaction details
              interaction:
                - trigger: "{{User action}}"
                  behavior: "{{UI response}}"

              # RECOMMENDED: Concrete examples
              examples:
                - input: "{{Example input data}}"
                  expected: "{{Expected output or behavior}}"

          # ============================================================
          # API & Dependency Tracking
          # ============================================================
          provides_apis: []     # Backend: ["POST /api/users"]
          consumes_apis: []     # Frontend/Mobile: ["POST /api/auth/login"]
          dependencies: []      # Story IDs: ["1.1", "1.2"]

          # ============================================================
          # SM Hints - Populated by UX-Expert and Architect AFTER PM creates Epic
          # ============================================================
          sm_hints:
            front_end_spec: null
            architecture: null
      ```

      ## BROWNFIELD-SPECIFIC REQUIREMENTS

      In addition to the standard enhanced AC structure above, brownfield stories MUST ensure:

      1. **acceptance_criteria** MUST include ACs covering these three categories:
         - **New functionality**: What the new feature does (the enhancement itself)
         - **Integration verification**: Existing functionality X still works correctly
         - **Performance impact**: Response time/memory within acceptable range

      2. **reuse_analysis** is STRONGLY RECOMMENDED for brownfield (not just post-MVP):
         - Identify existing code that can be reused directly
         - Flag code that needs extension
         - Document any conflicts with existing patterns
         - Reference existing files as pattern guides for new code

      3. **APIs tracking**:
         - Include BOTH new APIs and modified existing APIs
         - Mark existing APIs being enhanced with comment

      4. **dependencies**: Include both:
         - Story dependencies (which stories must complete first)
         - External dependencies (existing services that must be running)

      ## Field Requirements Reference

      **Epic-Level Fields:**

      | Field | Required | Condition | Description |
      |-------|----------|-----------|-------------|
      | epic_id | YES | Always | Unique epic identifier |
      | title | YES | Always | Epic title |
      | description | YES | Always | 2-3 sentences describing goal and value |
      | reuse_analysis | RECOMMENDED | Brownfield (always useful) | Existing implementation analysis |

      **Acceptance Criteria Fields:**

      | Field | Required | Condition | Description |
      |-------|----------|-----------|-------------|
      | id | YES | Always | AC identifier: AC1, AC2, etc. |
      | title | YES | Always | Concise AC title (5-10 words) |
      | scenario | YES | Always | GIVEN/WHEN/THEN structure |
      | business_rules | YES | Always | Minimum 1 rule per AC |
      | data_validation | CONDITIONAL | When AC has form/API input | Field-level validation specs |
      | error_handling | YES | Always | Minimum 1 error scenario |
      | interaction | NO | Only for UI stories | UI behavior details |
      | examples | RECOMMENDED | Always | Specification by Example |

      ## Complete Example (Brownfield multi-repo enhancement)

      ## Epic 1: AI-Powered Search Enhancement

      **Epic Summary:** Add AI-powered semantic search while maintaining backward compatibility.

      **Target Repositories:** backend, frontend

      **Integration Notes:** Enhances existing search without breaking current API contracts.

      ```yaml
      epic_id: 1
      title: "AI-Powered Search Enhancement"
      description: |
        Enhance existing search functionality with AI-powered semantic search.
        Backend adds new endpoints while maintaining existing search API.

      reuse_analysis:
        directly_reusable:
          - component: "SearchService"
            location: "src/services/search.service.ts"
            capability: "Keyword-based search with pagination and filtering"
            usage: "AI search will wrap this service, falling back to keyword search"
          - component: "SearchResultComponent"
            location: "src/components/SearchResult.tsx"
            capability: "Renders search results with pagination"
            usage: "Extend to show relevance scores when AI mode active"

        requires_extension:
          - component: "SearchController"
            location: "src/controllers/search.controller.ts"
            current_capability: "Handles GET /api/search endpoint"
            extension_needed: "Add POST /api/search/ai endpoint"
            affected_stories: ["1.1"]

        conflicts: []

        new_implementations:
          - feature: "AI Embedding Service"
            suggested_location: "src/services/ai-embedding.service.ts"
            pattern_reference: "src/services/search.service.ts"
            affected_stories: ["1.1"]

      stories:
        - id: "1.1"
          title: "Backend - AI Search Service Integration"
          repository_type: backend
          estimated_complexity: high
          priority: P0

          acceptance_criteria:
            - id: AC1
              title: "AI search returns relevant results"
              scenario:
                given: "Search index contains product data"
                when: "User submits semantic query via POST /api/search/ai"
                then:
                  - "Returns semantically relevant results ranked by relevance score"
                  - "Response includes relevance_score field per result"
                  - "Response format matches existing search API pagination"
              business_rules:
                - id: "BR-1.1"
                  rule: "AI search uses OpenAI embeddings for semantic matching"
                - id: "BR-1.2"
                  rule: "Results are limited to max 50 per page"
              data_validation:
                - field: "query"
                  type: "string"
                  required: true
                  rules: "1-500 characters, non-empty after trimming"
                  error_message: "Search query is required"
              error_handling:
                - scenario: "OpenAI API unavailable"
                  code: "503"
                  message: "AI search temporarily unavailable, please try keyword search"
                  action: "Log error, suggest fallback to keyword search"
              examples:
                - input: "POST /api/search/ai {\"query\": \"comfortable running shoes\"}"
                  expected: "200 OK with semantically matched products sorted by relevance"

            - id: AC2
              title: "Existing keyword search unchanged"
              scenario:
                given: "Existing search endpoint is deployed"
                when: "User queries GET /api/search?q=shoes"
                then:
                  - "Returns same results as before enhancement"
                  - "Response time within 10% of baseline"
                  - "All existing search tests pass"
              business_rules:
                - id: "BR-2.1"
                  rule: "Existing GET /api/search endpoint must not be modified"
              error_handling:
                - scenario: "Regression in existing search"
                  code: "500"
                  message: "Search service error"
                  action: "Rollback AI search changes, existing tests must pass"

          provides_apis:
            - "POST /api/search/ai"
            - "GET /api/search/ai/suggestions"
          consumes_apis:
            - "POST /external/openai/embeddings"
          dependencies: []
          sm_hints:
            front_end_spec: null
            architecture: null

        - id: "1.2"
          title: "Frontend - AI Search UI Component"
          repository_type: frontend
          estimated_complexity: medium
          priority: P0

          acceptance_criteria:
            - id: AC1
              title: "AI search toggle in search bar"
              scenario:
                given: "User is on any page with search bar"
                when: "User clicks AI search toggle"
                then:
                  - "Toggle switches between AI and keyword mode"
                  - "Visual indicator shows current mode"
                  - "Search input placeholder text updates"
              business_rules:
                - id: "BR-1.1"
                  rule: "Default mode is keyword search (backward compatible)"
                - id: "BR-1.2"
                  rule: "Toggle state persists in localStorage"
              error_handling:
                - scenario: "AI search API returns error"
                  code: "503"
                  message: "AI search unavailable. Switching to keyword search."
                  action: "Auto-fallback to keyword search, show toast notification"
              interaction:
                - trigger: "Toggle click"
                  behavior: "Smooth transition animation, placeholder text changes"
                - trigger: "AI search error"
                  behavior: "Show warning toast, auto-switch toggle to keyword mode"

            - id: AC2
              title: "Existing search works when AI disabled"
              scenario:
                given: "AI toggle is off (keyword mode)"
                when: "User performs search"
                then:
                  - "Uses existing keyword search endpoint"
                  - "UI behavior identical to pre-enhancement"
              business_rules:
                - id: "BR-2.1"
                  rule: "No visual or behavioral changes when AI is disabled"
              error_handling:
                - scenario: "Keyword search fails"
                  code: "500"
                  message: "Search temporarily unavailable"
                  action: "Show error state, preserve search query for retry"

          provides_apis: []
          consumes_apis:
            - "POST /api/search/ai"
            - "GET /api/search"
          dependencies:
            - "1.1"
          sm_hints:
            front_end_spec: null
            architecture: null
      ```

      ## Validation Checklist

      Before finalizing each Story, verify:

      - [ ] Every AC has at least 1 business rule
      - [ ] Every AC with data input has data_validation section
      - [ ] Every AC has at least 1 error_handling scenario
      - [ ] Examples cover both success and failure cases
      - [ ] UI stories have interaction section
      - [ ] All field error_message values are user-friendly
      - [ ] At least one AC verifies existing functionality still works (brownfield requirement)
      - [ ] reuse_analysis identifies existing code to leverage

      Now define YAML blocks for ALL epics for your brownfield enhancement.
    elicit: true