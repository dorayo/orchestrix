template:
  id: prd-template-v2
  name: Product Requirements Document
  version: 2.0
  output:
    format: markdown
    filename: docs/prd.md
    title: "{{project_name}} Product Requirements Document (PRD)"

workflow:
  mode: interactive
  elicitation: advanced-elicitation

sections:
  - id: goals-context
    title: Goals and Background Context
    instruction: |
      Ask if Project Brief document is available. If NO Project Brief exists, STRONGLY recommend creating one first using project-brief-tmpl (it provides essential foundation: problem statement, target users, success metrics, MVP scope, constraints). If user insists on PRD without brief, gather this information during Goals section. If Project Brief exists, review and use it to populate Goals (bullet list of desired outcomes) and Background Context (1-2 paragraphs on what this solves and why) so we can determine what is and is not in scope for PRD mvp. Either way this is critical to determine the requirements. Include Change Log table.
    sections:
      - id: goals
        title: Goals
        type: bullet-list
        instruction: Bullet list of 1 line desired outcomes the PRD will deliver if successful - user and project desires
      - id: background
        title: Background Context
        type: paragraphs
        instruction: 1-2 short paragraphs summarizing the background context, such as what we learned in the brief without being redundant with the goals, what and why this solves a problem, what the current landscape or need is
      - id: changelog
        title: Change Log
        type: table
        columns: [Date, Version, Description, Author]
        instruction: Track document versions and changes

  - id: requirements
    title: Requirements
    instruction: Draft the list of functional and non functional requirements under the two child sections
    elicit: true
    sections:
      - id: functional
        title: Functional
        type: numbered-list
        prefix: FR
        instruction: Each Requirement will be a bullet markdown and an identifier sequence starting with FR
        examples:
          - "FR6: The Todo List uses AI to detect and warn against potentially duplicate todo items that are worded differently."
      - id: non-functional
        title: Non Functional
        type: numbered-list
        prefix: NFR
        instruction: Each Requirement will be a bullet markdown and an identifier sequence starting with NFR
        examples:
          - "NFR1: AWS service usage must aim to stay within free-tier limits where feasible."

  - id: ui-goals
    title: User Interface Design Goals
    condition: PRD has UX/UI requirements
    instruction: |
      Capture high-level UI/UX vision to guide Design Architect and to inform story creation. Steps:
      
      1. Pre-fill all subsections with educated guesses based on project context
      2. Present the complete rendered section to user
      3. Clearly let the user know where assumptions were made
      4. Ask targeted questions for unclear/missing elements or areas needing more specification
      5. This is NOT detailed UI spec - focus on product vision and user goals
    elicit: true
    choices:
      accessibility: [None, WCAG AA, WCAG AAA]
      platforms: [Web Responsive, Mobile Only, Desktop Only, Cross-Platform]
    sections:
      - id: ux-vision
        title: Overall UX Vision
      - id: interaction-paradigms
        title: Key Interaction Paradigms
      - id: core-screens
        title: Core Screens and Views
        instruction: From a product perspective, what are the most critical screens or views necessary to deliver the the PRD values and goals? This is meant to be Conceptual High Level to Drive Rough Epic or User Stories
        examples:
          - "Login Screen"
          - "Main Dashboard"
          - "Item Detail Page"
          - "Settings Page"
      - id: accessibility
        title: "Accessibility: {None|WCAG AA|WCAG AAA|Custom Requirements}"
      - id: branding
        title: Branding
        instruction: Any known branding elements or style guides that must be incorporated?
        examples:
          - "Replicate the look and feel of early 1900s black and white cinema, including animated effects replicating film damage or projector glitches during page or state transitions."
          - "Attached is the full color pallet and tokens for our corporate branding."
      - id: target-platforms
        title: "Target Device and Platforms: {Web Responsive|Mobile Only|Desktop Only|Cross-Platform}"
        examples:
          - "Web Responsive, and all mobile platforms"
          - "iPhone Only"
          - "ASCII Windows Desktop"

  - id: technical-assumptions
    title: Technical Assumptions
    instruction: |
      Gather technical decisions that will guide the Architect. Steps:
      
      1. Check if {root}/data/technical-preferences.yaml or an attached technical-preferences file exists - use it to pre-populate choices
      2. Ask user about: languages, frameworks, starter templates, libraries, APIs, deployment targets
      3. For unknowns, offer guidance based on project goals and MVP scope
      4. Document ALL technical choices with rationale (why this choice fits the project)
      5. These become constraints for the Architect - be specific and complete
    elicit: true
    choices:
      repository: [Monorepo, Polyrepo]
      architecture: [Monolith, Microservices, Serverless]
      testing: [Unit Only, Unit + Integration, Full Testing Pyramid]
    sections:
      - id: repository-structure
        title: "Repository Structure: {Monorepo|Polyrepo|Multi-repo}"
      - id: service-architecture
        title: Service Architecture
        instruction: "CRITICAL DECISION - Document the high-level service architecture (e.g., Monolith, Microservices, Serverless functions within a Monorepo)."
      - id: testing-requirements
        title: Testing Requirements
        instruction: "CRITICAL DECISION - Document the testing requirements, unit only, integration, e2e, manual, need for manual testing convenience methods)."
      - id: repository-details
        title: Repository Details
        condition: repository-structure is Polyrepo or Multi-repo
        instruction: |
          For multi-repository projects, define all target repositories.
          This helps coordinate development across multiple teams and codebases.

          **For Monolith Projects**: Skip this section or state "Single repository (monolith architecture)"

          **For Multi-Repo Projects**: List all repositories with:
          - Repository name (following naming convention: {project}-{type})
          - Repository type (backend/frontend/ios/android/mobile/shared)
          - Primary technology (high-level: Node.js, React, Swift, Kotlin, etc.)
          - Team ownership (Backend Team, Frontend Team, Mobile Team, etc.)

          This information will be used by:
          - Architect to generate system-architecture.md
          - PO to map stories to repositories during epic sharding
          - SM to create stories in correct repositories
        elicit: true
        type: table
        columns: [Repository Name, Type, Technology, Team]
        examples:
          - "| my-ecommerce-backend | backend | Node.js + Express | Backend Team |"
          - "| my-ecommerce-web | frontend | React + Next.js | Frontend Team |"
          - "| my-ecommerce-ios | ios | Swift + SwiftUI | Mobile Team |"
      - id: additional-assumptions
        title: Additional Technical Assumptions and Requests
        instruction: Throughout the entire process of drafting this document, if any other technical assumptions are raised or discovered appropriate for the architect, add them here as additional bulleted items

  - id: epic-list
    title: Epic List
    instruction: |
      Present a high-level list of all epics for user approval. Each epic should have a title and a short (1 sentence) goal statement. This allows the user to review the overall structure before diving into details.
      
      CRITICAL: Epics MUST be logically sequential following agile best practices:
      
      - Each epic should deliver a significant, end-to-end, fully deployable increment of testable functionality
      - Epic 1 must establish foundational project infrastructure (app setup, Git, CI/CD, core services) unless we are adding new functionality to an existing app, while also delivering an initial piece of functionality, even as simple as a health-check route or display of a simple canary page - remember this when we produce the stories for the first epic!
      - Each subsequent epic builds upon previous epics' functionality delivering major blocks of functionality that provide tangible value to users or business when deployed
      - Not every project needs multiple epics, an epic needs to deliver value. For example, an API completed can deliver value even if a UI is not complete and planned for a separate epic.
      - Err on the side of less epics, but let the user know your rationale and offer options for splitting them if it seems some are too large or focused on disparate things.
      - Cross Cutting Concerns should flow through epics and stories and not be final stories. For example, adding a logging framework as a last story of an epic, or at the end of a project as a final epic or story would be terrible as we would not have logging from the beginning.
    elicit: true
    examples:
      - "Epic 1: Foundation & Core Infrastructure: Establish project setup, authentication, and basic user management"
      - "Epic 2: Core Business Entities: Create and manage primary domain objects with CRUD operations"
      - "Epic 3: User Workflows & Interactions: Enable key user journeys and business processes"
      - "Epic 4: Reporting & Analytics: Provide insights and data visualization for users"

  - id: epics
    title: Epics
    instruction: |
      ⚠️  CRITICAL SECTION - This section defines all Epics and Stories for the project.

      After the Epic List is approved, define each Epic with its Stories using structured YAML format.
      During PO *shard, this PRD will be split into docs/prd/*.md and the YAML blocks
      will be extracted to docs/prd/epic-{n}-{title}.yaml for SM agents to read.

      CRITICAL STORY SEQUENCING REQUIREMENTS:

      - Stories within each epic MUST be logically sequential
      - Each story should be a "vertical slice" delivering complete functionality
      - No story should depend on work from a later story or epic
      - Focus on "what" and "why" not "how" (leave technical implementation to Architect)
      - Size stories for AI agent execution: completable in one focused session
      - Think "junior developer working for 2-4 hours" - small, focused, self-contained

      For EACH epic, create a YAML block using this format:

      ## Epic {{N}}: {{Epic Title}}

      **Epic Summary:** {{2-3 sentence summary}}

      **Target Repositories:** {{backend, frontend, ios, android, mobile - as applicable}}

      ```yaml
      epic_id: {{N}}
      title: "{{Epic Title}}"
      description: |
        {{Epic goal - 2-3 sentences describing objective and value}}

      stories:
        - id: "{{N}}.1"
          title: "{{Story title}}"
          repository_type: backend | frontend | ios | android | monolith
          acceptance_criteria:
            - "AC1: {{Clear, testable criterion}}"
            - "AC2: {{Another criterion}}"
            - "AC3: {{And so on}}"
          estimated_complexity: low | medium | high
          priority: P0 | P1 | P2
          provides_apis: []    # Backend: ["POST /api/users", "GET /api/users/:id"]
          consumes_apis: []    # Frontend/Mobile: ["POST /api/auth/login"]
          dependencies: []     # Story IDs: ["1.1", "1.2"]
      ```

      FIELD REQUIREMENTS:

      1. **repository_type** (MANDATORY): Determines which repo gets each story
         - backend, frontend, ios, android, flutter, react-native, monolith

      2. **acceptance_criteria** (MANDATORY): Numbered list of testable criteria
         - Each starts with "ACn:" prefix
         - Clear, unambiguous, verifiable
         - Defines what "done" means from functional perspective

      3. **APIs tracking** (for multi-repo):
         - Backend: list APIs in `provides_apis`
         - Frontend/Mobile: list APIs in `consumes_apis`
         - Format: "METHOD /path" (e.g., "POST /api/users")

      4. **dependencies**: Story IDs this depends on (blocking until Done)

      EXAMPLE (Multi-repo project):

      ## Epic 1: User Authentication System

      **Epic Summary:** Implement complete user authentication across backend API and frontend UI.

      **Target Repositories:** backend, frontend

      ```yaml
      epic_id: 1
      title: "User Authentication System"
      description: |
        Establish foundation and core infrastructure for user authentication.
        Backend provides secure API endpoints. Frontend provides intuitive UI.

      stories:
        - id: "1.1"
          title: "Backend - User Registration and Login API"
          repository_type: backend
          acceptance_criteria:
            - "AC1: User can register with email and password"
            - "AC2: Password is hashed using bcrypt before storage"
            - "AC3: Login returns JWT token on valid credentials"
            - "AC4: Invalid credentials return 401 with error message"
            - "AC5: All endpoints return proper HTTP status codes"
          estimated_complexity: medium
          priority: P0
          provides_apis:
            - "POST /api/users"
            - "POST /api/auth/login"
            - "POST /api/auth/logout"
            - "GET /api/auth/me"
          consumes_apis: []
          dependencies: []

        - id: "1.2"
          title: "Frontend - User Registration and Login UI"
          repository_type: frontend
          acceptance_criteria:
            - "AC1: Registration form with email/password fields and validation"
            - "AC2: Login form calls backend API on submit"
            - "AC3: Successful login stores token and redirects to dashboard"
            - "AC4: Errors are displayed clearly to user"
            - "AC5: Forms are responsive and accessible (WCAG AA)"
          estimated_complexity: medium
          priority: P0
          provides_apis: []
          consumes_apis:
            - "POST /api/users"
            - "POST /api/auth/login"
            - "GET /api/auth/me"
          dependencies:
            - "1.1"
      ```

      Now define YAML blocks for ALL epics from your Epic List above.
    elicit: true

  - id: checklist-results
    title: Checklist Results Report
    instruction: Before running the checklist and drafting the prompts, offer to output the full updated PRD. If outputting it, confirm with the user that you will be proceeding to run the checklist and produce the report. Once the user confirms, execute the pm-checklist and populate the results in this section.

  - id: next-steps
    title: Next Steps
    sections:
      - id: ux-expert-prompt
        title: UX Expert Prompt
        instruction: This section will contain the prompt for the UX Expert, keep it short and to the point to initiate create architecture mode using this document as input.
      - id: architect-prompt
        title: Architect Prompt
        instruction: This section will contain the prompt for the Architect, keep it short and to the point to initiate create architecture mode using this document as input.