template:
  id: prd-template-v2
  name: Product Requirements Document
  version: 2.0
  output:
    format: markdown
    filename: docs/prd.md
    title: "{{project_name}} Product Requirements Document (PRD)"

workflow:
  mode: interactive
  elicitation: advanced-elicitation

sections:
  - id: goals-context
    title: Goals and Background Context
    instruction: |
      Ask if Project Brief document is available. If NO Project Brief exists, STRONGLY recommend creating one first using project-brief-tmpl (it provides essential foundation: problem statement, target users, success metrics, MVP scope, constraints). If user insists on PRD without brief, gather this information during Goals section. If Project Brief exists, review and use it to populate Goals (bullet list of desired outcomes) and Background Context (1-2 paragraphs on what this solves and why) so we can determine what is and is not in scope for PRD mvp. Either way this is critical to determine the requirements. Include Change Log table.
    sections:
      - id: goals
        title: Goals
        type: bullet-list
        instruction: Bullet list of 1 line desired outcomes the PRD will deliver if successful - user and project desires
      - id: background
        title: Background Context
        type: paragraphs
        instruction: 1-2 short paragraphs summarizing the background context, such as what we learned in the brief without being redundant with the goals, what and why this solves a problem, what the current landscape or need is
      - id: changelog
        title: Change Log
        type: table
        columns: [Date, Version, Description, Author]
        instruction: Track document versions and changes

  - id: requirements
    title: Requirements
    instruction: Draft the list of functional and non functional requirements under the two child sections
    elicit: true
    sections:
      - id: functional
        title: Functional
        type: numbered-list
        prefix: FR
        instruction: Each Requirement will be a bullet markdown and an identifier sequence starting with FR
        examples:
          - "FR6: The Todo List uses AI to detect and warn against potentially duplicate todo items that are worded differently."
      - id: non-functional
        title: Non Functional
        type: numbered-list
        prefix: NFR
        instruction: Each Requirement will be a bullet markdown and an identifier sequence starting with NFR
        examples:
          - "NFR1: AWS service usage must aim to stay within free-tier limits where feasible."

  - id: ui-goals
    title: User Interface Design Goals
    condition: PRD has UX/UI requirements
    instruction: |
      Capture high-level UI/UX vision to guide Design Architect and to inform story creation. Steps:
      
      1. Pre-fill all subsections with educated guesses based on project context
      2. Present the complete rendered section to user
      3. Clearly let the user know where assumptions were made
      4. Ask targeted questions for unclear/missing elements or areas needing more specification
      5. This is NOT detailed UI spec - focus on product vision and user goals
    elicit: true
    choices:
      accessibility: [None, WCAG AA, WCAG AAA]
      platforms: [Web Responsive, Mobile Only, Desktop Only, Cross-Platform]
    sections:
      - id: ux-vision
        title: Overall UX Vision
      - id: interaction-paradigms
        title: Key Interaction Paradigms
      - id: core-screens
        title: Core Screens and Views
        instruction: From a product perspective, what are the most critical screens or views necessary to deliver the the PRD values and goals? This is meant to be Conceptual High Level to Drive Rough Epic or User Stories
        examples:
          - "Login Screen"
          - "Main Dashboard"
          - "Item Detail Page"
          - "Settings Page"
      - id: accessibility
        title: "Accessibility: {None|WCAG AA|WCAG AAA|Custom Requirements}"
      - id: branding
        title: Branding
        instruction: Any known branding elements or style guides that must be incorporated?
        examples:
          - "Replicate the look and feel of early 1900s black and white cinema, including animated effects replicating film damage or projector glitches during page or state transitions."
          - "Attached is the full color pallet and tokens for our corporate branding."
      - id: target-platforms
        title: "Target Device and Platforms: {Web Responsive|Mobile Only|Desktop Only|Cross-Platform}"
        examples:
          - "Web Responsive, and all mobile platforms"
          - "iPhone Only"
          - "ASCII Windows Desktop"

  - id: technical-assumptions
    title: Technical Assumptions
    instruction: |
      Gather technical decisions that will guide the Architect. Steps:
      
      1. Check if {root}/data/technical-preferences.yaml or an attached technical-preferences file exists - use it to pre-populate choices
      2. Ask user about: languages, frameworks, starter templates, libraries, APIs, deployment targets
      3. For unknowns, offer guidance based on project goals and MVP scope
      4. Document ALL technical choices with rationale (why this choice fits the project)
      5. These become constraints for the Architect - be specific and complete
    elicit: true
    choices:
      repository: [Monorepo, Polyrepo]
      architecture: [Monolith, Microservices, Serverless]
      testing: [Unit Only, Unit + Integration, Full Testing Pyramid]
    sections:
      - id: repository-structure
        title: "Repository Structure: {Monorepo|Polyrepo|Multi-repo}"
      - id: service-architecture
        title: Service Architecture
        instruction: "CRITICAL DECISION - Document the high-level service architecture (e.g., Monolith, Microservices, Serverless functions within a Monorepo)."
      - id: testing-requirements
        title: Testing Requirements
        instruction: "CRITICAL DECISION - Document the testing requirements, unit only, integration, e2e, manual, need for manual testing convenience methods)."
      - id: repository-details
        title: Repository Details
        condition: repository-structure is Polyrepo or Multi-repo
        instruction: |
          For multi-repository projects, define all target repositories.
          This helps coordinate development across multiple teams and codebases.

          **For Monolith Projects**: Skip this section or state "Single repository (monolith architecture)"

          **For Multi-Repo Projects**: List all repositories with:
          - Repository name (following naming convention: {project}-{type})
          - Repository type (backend/frontend/ios/android/mobile/shared)
          - Primary technology (high-level: Node.js, React, Swift, Kotlin, etc.)
          - Team ownership (Backend Team, Frontend Team, Mobile Team, etc.)

          This information will be used by:
          - Architect to generate system-architecture.md
          - PO to map stories to repositories during epic sharding
          - SM to create stories in correct repositories
        elicit: true
        type: table
        columns: [Repository Name, Type, Technology, Team]
        examples:
          - "| my-ecommerce-backend | backend | Node.js + Express | Backend Team |"
          - "| my-ecommerce-web | frontend | React + Next.js | Frontend Team |"
          - "| my-ecommerce-ios | ios | Swift + SwiftUI | Mobile Team |"
      - id: additional-assumptions
        title: Additional Technical Assumptions and Requests
        instruction: Throughout the entire process of drafting this document, if any other technical assumptions are raised or discovered appropriate for the architect, add them here as additional bulleted items

  - id: epic-list
    title: Epic List
    instruction: |
      Present a high-level list of all epics for user approval. Each epic should have a title and a short (1 sentence) goal statement. This allows the user to review the overall structure before diving into details.
      
      CRITICAL: Epics MUST be logically sequential following agile best practices:
      
      - Each epic should deliver a significant, end-to-end, fully deployable increment of testable functionality
      - Epic 1 must establish foundational project infrastructure (app setup, Git, CI/CD, core services) unless we are adding new functionality to an existing app, while also delivering an initial piece of functionality, even as simple as a health-check route or display of a simple canary page - remember this when we produce the stories for the first epic!
      - Each subsequent epic builds upon previous epics' functionality delivering major blocks of functionality that provide tangible value to users or business when deployed
      - Not every project needs multiple epics, an epic needs to deliver value. For example, an API completed can deliver value even if a UI is not complete and planned for a separate epic.
      - Err on the side of less epics, but let the user know your rationale and offer options for splitting them if it seems some are too large or focused on disparate things.
      - Cross Cutting Concerns should flow through epics and stories and not be final stories. For example, adding a logging framework as a last story of an epic, or at the end of a project as a final epic or story would be terrible as we would not have logging from the beginning.
    elicit: true
    examples:
      - "Epic 1: Foundation & Core Infrastructure: Establish project setup, authentication, and basic user management"
      - "Epic 2: Core Business Entities: Create and manage primary domain objects with CRUD operations"
      - "Epic 3: User Workflows & Interactions: Enable key user journeys and business processes"
      - "Epic 4: Reporting & Analytics: Provide insights and data visualization for users"

  - id: epic-details
    title: Epic {{epic_number}} {{epic_title}}
    repeatable: true
    instruction: |
      After the epic list is approved, present each epic with all its stories and acceptance criteria as a complete review unit.
      
      For each epic provide expanded goal (2-3 sentences describing the objective and value all the stories will achieve).
      
      CRITICAL STORY SEQUENCING REQUIREMENTS:
      
      - Stories within each epic MUST be logically sequential
      - Each story should be a "vertical slice" delivering complete functionality aside from early enabler stories for project foundation
      - No story should depend on work from a later story or epic
      - Identify and note any direct prerequisite stories
      - Focus on "what" and "why" not "how" (leave technical implementation to Architect) yet be precise enough to support a logical sequential order of operations from story to story.
      - Ensure each story delivers clear user or business value, try to avoid enablers and build them into stories that deliver value.
      - Size stories for AI agent execution: Each story must be completable by a single AI agent in one focused session without context overflow
      - Think "junior developer working for 2-4 hours" - stories must be small, focused, and self-contained
      - If a story seems complex, break it down further as long as it can deliver a vertical slice
    elicit: true
    template: "{{epic_goal}}"
    sections:
      - id: story
        title: Story {{epic_number}}.{{story_number}} {{story_title}}
        repeatable: true
        template: |
          As a {{user_type}},
          I want {{action}},
          so that {{benefit}}.
        sections:
          - id: target-platform
            title: Target Platform
            type: choice
            choices: [backend, frontend, ios, android, mobile, fullstack, monolith]
            default: fullstack
            condition: project is multi-repo
            instruction: |
              **MULTI-REPO ONLY**: Specify which repository/platform this story targets.

              Options:
              - **backend**: Backend API implementation (database, business logic, API endpoints)
              - **frontend**: Web frontend (React, Vue, Angular, etc.)
              - **ios**: iOS mobile app (Swift/SwiftUI)
              - **android**: Android mobile app (Kotlin/Jetpack Compose)
              - **mobile**: Cross-platform mobile (Flutter/React Native)
              - **fullstack**: Story spans multiple repositories (avoid if possible, prefer splitting)
              - **monolith**: For monolith projects (single repository)

              **Recommendation**: Prefer single-repo stories (backend, frontend, ios, android) over fullstack.
              If a story needs both frontend and backend, consider splitting into two stories with dependencies.
            elicit: true
          - id: repository-name
            title: Repository Name
            type: string
            condition: target-platform is not monolith
            instruction: |
              **MULTI-REPO ONLY**: Specify the repository name for this story.

              Examples:
              - `my-ecommerce-backend` (for backend stories)
              - `my-ecommerce-web` (for frontend stories)
              - `my-ecommerce-ios` (for iOS stories)

              Leave empty for monolith projects.
              For fullstack stories, specify the primary repository.

              **Note**: This will be used by PO during epic sharding to generate Epic YAML files.
            elicit: false
          - id: provides-apis
            title: APIs Provided
            type: list
            condition: target-platform is backend
            instruction: |
              **BACKEND STORIES ONLY**: List APIs this story will implement.

              Format: "METHOD /path"

              Examples:
              - "POST /api/users"
              - "GET /api/users/:id"
              - "PUT /api/users/:id"
              - "DELETE /api/users/:id"

              **Purpose**: This information is used to:
              - Generate API contract documents
              - Track cross-repo dependencies (frontend/mobile stories that consume these APIs)
              - Validate API completeness during QA review
            elicit: true
          - id: consumes-apis
            title: APIs Consumed
            type: list
            condition: target-platform in [frontend, ios, android, mobile]
            instruction: |
              **FRONTEND/MOBILE STORIES ONLY**: List APIs this story will consume.

              Format: "METHOD /path"

              Examples:
              - "POST /api/auth/login"
              - "GET /api/products"
              - "GET /api/products/:id"

              **Purpose**: This information is used to:
              - Validate that required backend APIs are implemented
              - Track cross-repo dependencies (this story depends on backend stories)
              - Generate API contract validation tests
            elicit: true
          - id: cross-repo-dependencies
            title: Cross-Repository Dependencies
            type: list
            condition: project is multi-repo
            instruction: |
              **MULTI-REPO ONLY**: List other stories this depends on (may be in different repos).

              Format: "Story ID - Brief Description"

              Examples:
              - "1.1 - Backend User API must be complete"
              - "2.3 - Authentication service must be deployed"

              **Purpose**: This information is used to:
              - SM agent will check dependencies before allowing story creation
              - PO will validate dependency graph during epic sharding
              - QA will verify dependencies are satisfied

              **Note**: Dependencies create blocking relationships. Story X cannot start until all its dependencies are Status = Done.
            elicit: false
          - id: acceptance-criteria
            title: Acceptance Criteria
            type: numbered-list
            item_template: "{{criterion_number}}: {{criteria}}"
            repeatable: true
            instruction: |
              Define clear, comprehensive, and testable acceptance criteria that:

              - Precisely define what "done" means from a functional perspective
              - Are unambiguous and serve as basis for verification
              - Include any critical non-functional requirements from the PRD
              - Consider local testability for backend/data components
              - Specify UI/UX requirements and framework adherence where applicable
              - Avoid cross-cutting concerns that should be in other stories or PRD sections

  - id: epic-planning
    title: Epic Planning
    instruction: |
      ⚠️  CRITICAL SECTION - This section will be used for story creation across all repositories.

      This section consolidates all epic and story definitions into structured YAML format.
      During PO *shard, this PRD will be split into docs/prd/*.md using md-tree,
      and the YAML blocks below will be preserved in the sharded files for SM agents to read.

      For EACH epic defined in the "Epic Details" section above, create a corresponding
      YAML block below using this exact format:

      ## Epic {{N}}: {{Epic Title from Epic Details}}

      **Epic Summary:** {{2-3 sentence summary from epic details}}

      **Target Repositories:** {{List applicable repository types: backend, frontend, ios, android, mobile}}

      ```yaml
      epic_id: {{N}}
      title: "{{Epic Title}}"
      description: |
        {{Epic goal and description from epic details section}}

      stories:
        - id: "{{N}}.1"
          title: "{{Story title from Epic Details}}"
          repository_type: {{backend|frontend|ios|android|flutter|react-native|monolith}}
          acceptance_criteria_summary: |
            {{Consolidate all ACs from Epic Details into paragraph form}}
          estimated_complexity: {{low|medium|high}}
          priority: {{P0|P1|P2}}
          provides_apis: []  # For backend stories: list of "METHOD /path"
          consumes_apis: []  # For frontend/mobile stories: list of "METHOD /path"
          cross_repo_dependencies: []  # List of "Story ID - Description" if any

        - id: "{{N}}.2"
          title: "{{Next story title}}"
          repository_type: {{backend|frontend|ios|android|flutter|react-native|monolith}}
          acceptance_criteria_summary: |
            {{Consolidate ACs}}
          estimated_complexity: {{low|medium|high}}
          priority: {{P0|P1|P2}}
          provides_apis: []
          consumes_apis: []
          cross_repo_dependencies: []
      ```

      IMPORTANT INSTRUCTIONS:

      1. **One YAML block per epic** - Do not combine multiple epics in one YAML block
      2. **repository_type is MANDATORY** - This field determines which implementation repo gets each story
      3. **Maintain epic/story IDs** - Must match the IDs from Epic Details section (Epic 1 → epic_id: 1, Story 1.3 → id: "1.3")
      4. **APIs tracking**:
         - Backend stories MUST list APIs they provide in provides_apis
         - Frontend/mobile stories MUST list APIs they consume in consumes_apis
         - Use format: "METHOD /path" (e.g., "POST /api/users", "GET /api/users/:id")
      5. **Cross-repo dependencies**:
         - If a story depends on another story (possibly in different repo), list in cross_repo_dependencies
         - Format: "Story ID - Brief description" (e.g., "1.2 - Backend auth API must be complete")
      6. **Acceptance criteria consolidation**:
         - Convert numbered AC list from Epic Details into flowing paragraph form
         - Keep all critical acceptance points but make it readable as a summary
      7. **For monolith projects**: Use repository_type: monolith for all stories

      EXAMPLE (Multi-repo project):

      ## Epic 1: User Authentication System

      **Epic Summary:** Implement complete user authentication across backend API and frontend UI, including registration, login, and session management.

      **Target Repositories:** backend, frontend

      ```yaml
      epic_id: 1
      title: "User Authentication System"
      description: |
        Establish foundation and core infrastructure for user authentication.
        Backend will provide secure API endpoints for user registration and login.
        Frontend will provide intuitive UI for authentication flows.

      stories:
        - id: "1.1"
          title: "Backend - User Registration and Login API"
          repository_type: backend
          acceptance_criteria_summary: |
            User can register with email and password. Password must be hashed using bcrypt.
            User can login with correct credentials and receive JWT token. Invalid credentials
            return appropriate error. All endpoints return proper HTTP status codes and error messages.
          estimated_complexity: medium
          priority: P0
          provides_apis:
            - "POST /api/users"
            - "POST /api/auth/login"
            - "POST /api/auth/logout"
            - "GET /api/auth/me"
          consumes_apis: []
          cross_repo_dependencies: []

        - id: "1.2"
          title: "Frontend - User Registration and Login UI"
          repository_type: frontend
          acceptance_criteria_summary: |
            Registration form with email and password fields, with client-side validation.
            Login form with email and password fields. Form submission calls backend API.
            Successful login stores token and redirects to dashboard. Errors are displayed
            clearly to the user. Forms are responsive and accessible (WCAG AA).
          estimated_complexity: medium
          priority: P0
          provides_apis: []
          consumes_apis:
            - "POST /api/users"
            - "POST /api/auth/login"
            - "GET /api/auth/me"
          cross_repo_dependencies:
            - "1.1 - Backend auth API must be complete"
      ```

      Now create YAML blocks for ALL epics defined in your Epic Details section above.

  - id: checklist-results
    title: Checklist Results Report
    instruction: Before running the checklist and drafting the prompts, offer to output the full updated PRD. If outputting it, confirm with the user that you will be proceeding to run the checklist and produce the report. Once the user confirms, execute the pm-checklist and populate the results in this section.

  - id: next-steps
    title: Next Steps
    sections:
      - id: ux-expert-prompt
        title: UX Expert Prompt
        instruction: This section will contain the prompt for the UX Expert, keep it short and to the point to initiate create architecture mode using this document as input.
      - id: architect-prompt
        title: Architect Prompt
        instruction: This section will contain the prompt for the Architect, keep it short and to the point to initiate create architecture mode using this document as input.